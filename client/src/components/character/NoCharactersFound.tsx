import classes from './NoCharactersFound.module.css';

const NoCharactersFound = () => {
  return (
    <div className={classes.noquotes}>
      <p>No characters found!</p>
    </div>
  );
};

export default NoCharactersFound;
